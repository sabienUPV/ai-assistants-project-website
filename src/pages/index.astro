---
// This is empty on purpose.
// The i18n configuration needs this file to be present,
// but it will automatically redirect to the correct locale based on the URL prefix (e.g. /en, /es, etc.).
---