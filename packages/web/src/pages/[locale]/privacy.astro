---
import { getStaticPathsFromLocales as getStaticPaths } from '@utils/translation';

export { getStaticPaths };

import Layout from "@layouts/Layout.astro";

const { t, homeLocale } = Astro.locals;
const imprintUrl = `${homeLocale()}imprint`;
---

<Layout title={t('privacy_title')}>
  <main class="privacy-container">
    <h1>{t('privacy_title')}</h1>

    {/* 
      Using set:html to render HTML content with glossaries and bolded text:
      - intro_text: Glossary for "GDPR".
      - analytics_text1: Uses "<strong>" for referencing cookies, and glossary for "cookies" as well.
      - analytics_text2 and analytics_text3: Glossary for "IP address".
      - rights_text: Glossary for "GDPR".
    */}

    <section class="privacy-section">
      <h2>{t('privacy_intro_title')}</h2>
      <p set:html={t('privacy_intro_text', true)}></p>
    </section>

    <section class="privacy-section">
      <h2>{t('privacy_analytics_title')}</h2>
      <p set:html={t('privacy_analytics_text1', true)} />
      <p set:html={t('privacy_analytics_text2', true)} />
      <p set:html={t('privacy_analytics_text3_geoip', true)} />
      <p>{t('privacy_analytics_text4')}</p>
    </section>

    <section class="privacy-section">
      <h2>{t('privacy_hosting_title')}</h2>
      <p>{t('privacy_hosting_text')}</p>
    </section>

    <section class="privacy-section">
      <h2>{t('privacy_rights_title')}</h2>
      <p>
        <Fragment set:html={t('privacy_rights_text', true)} /> <a href={imprintUrl}>{t('imprint_title')}</a>.
      </p>
    </section>
  </main>
</Layout>

<style>
  .privacy-container {
    max-width: 800px;
    margin: 0 auto;
    padding: 3rem 1rem;
    color: var(--color-text);
  }

  h1 { font-size: 2.5rem; margin-bottom: 2.5rem; }
  h2 { font-size: 1.5rem; margin-bottom: 1.2rem; color: var(--color-secondary); border-bottom: 1px solid #eaeaea; padding-bottom: 0.5rem; }
  
  .privacy-section { margin-bottom: 3rem; }
  
  .privacy-section p {
    margin-bottom: 1rem;
    font-size: 1.05rem;
    line-height: 1.6;
  }

  a { color: var(--color-secondary); text-decoration: underline; }
  a:hover { text-decoration: none; }
</style>