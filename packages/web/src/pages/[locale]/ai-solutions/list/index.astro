---
import { getStaticPathsFromLocales as getStaticPaths } from '@utils/translation';
import { getFirstPageRewriteUrlPath } from '@utils/url';

export { getStaticPaths };

return Astro.rewrite(getFirstPageRewriteUrlPath(Astro.url.pathname));
---