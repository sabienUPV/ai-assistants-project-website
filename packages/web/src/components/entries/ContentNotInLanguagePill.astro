---
// IMPORTANT: Even though we are importing a Svelte component here, as long as we don't use client:load or similar directives when using the component,
// Astro will render it as a static HTML element, with 0 JavaScript, the same way as any other static content.
import ContentNotInLanguagePill from '@components/client/shared/ContentNotInLanguagePill.svelte';
const { t } = Astro.locals;
---

<ContentNotInLanguagePill message={t('content_not_available_in_your_language')} />